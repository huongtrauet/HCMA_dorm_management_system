<%= form_with(model: facility_report, local: true) do |form| %>
  <% if facility_report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(facility_report.errors.count, "error") %> prohibited this facility_report from being saved:</h2>

      <ul>
        <% facility_report.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
