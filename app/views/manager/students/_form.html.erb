<%= form_for [:manager, @student] do |form| %>
  <% if student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(student.errors.count, "error") %> prohibited this student from being saved:</h2>

      <ul>
        <% student.errors.messages.each do |message| %>
          <li><%= message[1][0] %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :student_id_number %>
    <%= form.text_field :student_id_number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </>
<% end %>
